# Server Configuration
PORT=3000
HOST=127.0.0.1              # Use 127.0.0.1 for local only, 0.0.0.0 for network access
SHELL=/bin/bash

# Workspace Configuration
# Default directory where new projects will be created
# Examples:
# DEFAULT_WORKSPACE=/home/username/projects         # Linux/macOS
# DEFAULT_WORKSPACE=/mnt/c/Users/username/projects  # WSL
DEFAULT_WORKSPACE=$HOME/projects

# Optional: AI Assistant Configuration
DEFAULT_LLM=claude          # Default LLM to use (claude, openai, etc.)

# Optional: Session Configuration
SESSION_TIMEOUT=86400       # Session timeout in seconds (default: 24 hours)
MAX_SESSIONS=50            # Maximum concurrent sessions

# Environment
NODE_ENV=production