# Server Configuration
PORT=9999
HOST=0.0.0.0
# For Tailscale or specific IPs, uncomment and set to your server's IP on VPN

# LLM Configuration (optional)
# DEFAULT_LLM=gemini  # Options: claude, openai, custom

# Paths
DEFAULT_WORKSPACE=~

# Security
NODE_ENV=production